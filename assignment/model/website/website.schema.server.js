var mongoose = require('mongoose');

var websiteSchema = mongoose.Schema({
    _user: ,
    name: String,
    description: String,
    pages: ,
    dateCreated: Date
});

module.exports = websiteSchema;
